import '@xterm/xterm/css/xterm.css'

export const metadata = {
  title: 'Terminal — Nataly Office',
}

export default function TerminalLayout({ children }: { children: React.ReactNode }) {
  return children
}
